$ GRAVITON
graviton            // Main graviton executable (MAIN INIT PROCESS PID 1)
init                // just a symlink to GRAVITON (this might change if some day another init/daemon was born)
gravitonctl         // System daemon manager
gravitonadm         // Security manager and audit

$ CORE UTILS
clear               // Clear current terminal
reset               // Reset current terminal
cdl                 // Current directory lists
cdir                // Create directory
rash                // A Shell
shutdown            // You guessed it, shutdown
reboot              // reboot
echo                // echo
acpi                // shows every bit about acpi
safemode            // Immidietly switch the kernel to safe mode
sys                 // System/Service/Kernel/Daemon API caller tool